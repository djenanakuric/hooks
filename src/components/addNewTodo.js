import React from "react";

const addNewTodo = () => {
    return(
        <form osSubmit ={}>
            <label htmlFor="todo">To do</label>
            <input type='text' id='todo' onChange = {} />
            <input type='submit' />
        </form>
    )
};

export default addNewTodo;